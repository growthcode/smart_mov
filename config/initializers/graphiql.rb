# TODO: update with devise... the below is set up with "clearance" gem
# GraphiQL::Rails.config.headers['Authorization'] = ->(context) {
#   "Token #{context.request.env[:clearance].current_user.try(:api_token)}"
# }
